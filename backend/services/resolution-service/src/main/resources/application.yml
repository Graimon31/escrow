server:
  port: 0

spring:
  application:
    name: resolution-service
  datasource:
    url: ${APP_RESOLUTION_DB_URL:jdbc:postgresql://postgres:5432/escrow}
    username: ${APP_RESOLUTION_DB_USER:escrow}
    password: ${APP_RESOLUTION_DB_PASSWORD:escrow}
  jpa:
    hibernate:
      ddl-auto: validate
  flyway:
    enabled: true
    table: flyway_schema_history_resolution
  kafka:
    bootstrap-servers: ${SPRING_KAFKA_BOOTSTRAP_SERVERS:kafka:9092}
    producer:
      key-serializer: org.apache.kafka.common.serialization.StringSerializer
      value-serializer: org.springframework.kafka.support.serializer.JsonSerializer

management:
  endpoints:
    web:
      exposure:
        include: health,info,prometheus

springdoc:
  swagger-ui:
    path: /swagger-ui.html

app:
  kafka:
    resolution-topic: ${APP_KAFKA_RESOLUTION_TOPIC:escrow.resolution.events}
  security:
    jwt-secret: ${APP_AUTH_JWT_SECRET:change-me-in-env-very-secret-key-1234567890}
