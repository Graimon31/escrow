server:
  port: 8082
spring:
  application:
    name: escrow-account-service
  kafka:
    bootstrap-servers: 
management:
  endpoints:
    web:
      exposure:
        include: health,info,prometheus
